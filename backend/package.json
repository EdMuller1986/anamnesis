{
  "name": "anamnesis-backend",
  "version": "0.1.0",
  "description": "Anamnesis backend — Cloudflare Worker (Hono + D1 + R2)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1090.0",
    "@aws-sdk/s3-request-presigner": "^3.1090.0",
    "@simplewebauthn/server": "^13.1.1",
    "hono": "^4.2.7"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  }
}
