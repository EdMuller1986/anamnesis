# Frontend environment variables template
# Copy this to .env.local and fill in your values

# API backend URL (for production builds)
# In development, Vite proxy will forward /api to localhost:3010
# In production, set this to your Cloudflare Workers URL
VITE_API_URL=https://your-backend.workers.dev/api
